services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mmm
      POSTGRES_PASSWORD: mmm
      POSTGRES_DB: mmm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
